My custom scripts for TORN

- `Bank Investment Return` calculates Torn bank investment returns with merits and the TCI bank-interest stock block.
- `Revive Service` provides a Google Apps Script backed revive request queue with requester and watcher userscripts.
- `Abroad Stock Watcher` watches selected foreign item stocks, notifies when they are available, and stores previous stocked times locally.
